<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Busmonitor.Views.MenuPage">
  <ContentPage.Content>
    <StackLayout BackgroundColor="Gray">
      <Label Text="{Binding Settings.IP}"/>
      <Label Text="{Binding Settings.InterfaceName}"/>
      <Button Text="Monitor" TextColor="White" BackgroundColor="Green" Command="{Binding GoMonitorCommand}" />
      <Button Text="Interfaces" TextColor="White" BackgroundColor="Navy" Command="{Binding GoInterfacesCommand}" />
      <Button Text="Security" TextColor="White" BackgroundColor="Navy" Command="{Binding GoSecurityCommand}" />
      <Button Text="Export" TextColor="White" BackgroundColor="Navy" Command="{Binding GoExportCommand}" />
    </StackLayout>
  </ContentPage.Content>
</ContentPage>