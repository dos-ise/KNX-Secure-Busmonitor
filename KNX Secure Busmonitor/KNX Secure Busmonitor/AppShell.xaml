<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        xmlns:d="http://xamarin.com/schemas/2014/forms/design"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:Busmonitor.Views;assembly=KNX Secure Busmonitor"
        mc:Ignorable="d"
        x:Class="Busmonitor.AppShell">
  <Shell.FlyoutHeader>
    <StackLayout Orientation="Vertical">
      <Label Text="{Binding Settings.IP}"/>
      <Label Text="{Binding Settings.InterfaceName}"/>
    </StackLayout>
  </Shell.FlyoutHeader>
  <FlyoutItem Title="Monitor">
    <ShellContent>
      <views:HomePage BindingContext="{Binding Home}"/>
    </ShellContent>
  </FlyoutItem>
  <FlyoutItem Title="Interfaces">
    <ShellContent>
      <views:Interfaces BindingContext="{Binding Interface}"/>
    </ShellContent>
  </FlyoutItem>
  <FlyoutItem Title="Security">
    <ShellContent>
      <views:SecurityPage BindingContext="{Binding Security}"/>
    </ShellContent>
  </FlyoutItem>
  <FlyoutItem Title="Export">
    <ShellContent>
      <views:ExportPage BindingContext="{Binding Export}"/>
    </ShellContent>
  </FlyoutItem>
  <FlyoutItem Title="Import GA">
    <ShellContent>
      <views:GroupAddressImportView BindingContext="{Binding GaImport}"/>
    </ShellContent>
  </FlyoutItem>
</Shell>